import { Context } from "telegraf";
import { createUser } from "../../models/User";

export async function handleStart(ctx: Context) {
  const user = ctx.from;
  if (!user) return;

  await createUser(user.id, user.username, user.first_name);

  await ctx.reply(
    `Привет, ${user.first_name}! 👋\n\n` +
      `Я бот для удобного сохранения полезных материалов 📌\n\n` +
      `Как это работает:\n` +
      `🔗 Отправь мне ссылку на пост, видео или музыку\n` +
      `📂 Выбери категорию\n` +
      `✏️ Добавь название (по желанию)\n\n` +
      `Я сохраню материал в приложении, и ты сможешь вернуться к нему в любой момент 🚀`
  );
}
