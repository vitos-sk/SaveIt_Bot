import { Context } from "telegraf";

export async function handleHelp(ctx: Context) {
  await ctx.reply(
    `📖 Справка по использованию бота:\n\n` +
      `💾 Сохранение материалов:\n` +
      `1. Отправьте ссылку на пост/музыку/видео (через «Копировать ссылку»)\n` +
      `   - Я сохраняю именно ссылку на конкретный пост\n` +
      `   - Материалы из директа (личных диалогов между людьми) не сохраняются\n` +
      `2. Выберите категорию кнопкой:\n` +
      `   💡 Идеи\n` +
      `   ✅ Задачи\n` +
      `   📚 База\n` +
      `   📝 Заметки\n` +
      `   🔖 Закладки\n` +
      `   💭 Вдохновение\n` +
      `   📖 Учеба\n` +
      `   🎮 Фан\n` +
      `3. Добавьте название или отправьте /skip\n\n` +
      `📋 Просмотр:\n` +
      `Используйте /links для просмотра сохранённых материалов\n\n`
  );
}
